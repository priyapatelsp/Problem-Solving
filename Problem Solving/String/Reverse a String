Reverse a String

https://www.geeksforgeeks.org/problems/reverse-a-string/1

---------------------------------------------------------------------

public static String reverseWord(String str)
    {
        int start=0;
        int end=str.length()-1;
        while(start<end){
            char temp=str.charAt(start);
            str.put(start,str.charAt(end));
            str.put(end,temp);
            start++;
            end--;
        }
    }
