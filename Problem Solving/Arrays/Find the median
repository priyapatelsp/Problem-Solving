Find the median

https://www.geeksforgeeks.org/problems/find-the-median0527/1
----------------------------------------------------------------

  public int find_median(int[] v)
    {  Arrays.sort(v);
        int n=v.length;
         int m=n/2;
        if(n%2==0)
        {
           return  (v[m-1] + v[m]) / 2;
        }
        else
        {
        return v[m]; 
        }
    }
